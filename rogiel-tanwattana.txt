Copy the questions below into the firt-lastname.txt file on your github repo - answer each question and then push your changes. 

    1. What is Semantic HTML? 
    -Semantic HTML is what enterprets the texts on a webpage.
    2. What is HTML used for? 
    - HTML is used to structure a webpage on the internet.
    3. What is an attribute and where do we put it? 
    - An Attribute is a modifyer, they are put in opening tags.
    4. What is the h1 tag used for? How many times should I use it on a page?
    - h1 is used for the size of the header, ideally you should have only 1 but you can put as many as you want.
    5. Name two tags that have required attributes
    -<img src="myimage.jpg">, <p style="color:red">
    6. What do we put in the head of our HTML document? 
    - <head> tag
    7. What is an id? 
    - An id specifies a unique id for an HTML element
    8. What elements can I add an id to? 
    - id can be used to any HTML element
    9. How many times can I use the same id on a page? 
    -No, element IDs should be different.
    10. What is a class? 
    -A class is used to define styles for elements from the same "class" name.
    11. What elements can I add a class to? 
    -Class can be used on any element
    12. How many times can I use the same class on a page? 
    - You can have more than one on a page
    13. How do I get my link to open in a new tab?
    - add target="_blank" attribute to your links
    14. What is the alt attribute used for? 
    - Alt attributes are applied to image tags.
    15. How do I reference an id?
    - To select a specific id, type # followed by the id of the element.
    16. What is the difference between a section and a div
    -<section> tag wraps up the content, while the <div> tagssplits it in half
    17. What is CSS used for? 
    -CSS is used for styling your webpage
    18. How to we select an element? 
    19. What is the difference between a class and an id?  
    -ID can be used to identify one element, Class can be used to identify more than one
    20. How do we select classes in CSS?
    -to select a specific class you put (.) followed by the name of the class
    21. How do we select a p element with a single class of “human””?
    22. What is a parent child selector? When would this be useful? 
    -Its used to match all the elements which are child of a specific element.
    23. How do you select all links within a div with the class of sidebar?
    -you need to put all the sidebar elements in a <div> with a .sidebar class
    24. What is a pseudo selector?
    -A psuedo selector is a kepword added to a selector that specifies the selected element
    25. What do we use the change the spacing between lines?
    -It is used for line height and spacings between the lines
    26. What do we use to change the spacing between letters?
    -Used for properly increasing or decreasing the space between characters in a text.
    27. What do we use to to change everything to CAPITALS? lowercase? Capitalize?
    -To change specific capitalizations or lowercase in a text, in a way you dont have to go back in through HTML to fix it.
    28. How do I add a 1px border around my div that is dotted and black?
    - border:1px dotted black;
    29. How do I select everything on the page? 
    - *{
    30. How do I write a comment in CSS?
    - /* comment /*
    31. How do I find out what file I am in, when I am using the command line? 
    - pwd
    32. Using the command line - how do I see a list of files/folders in my current folder?
    - ls -la
    33. How do I remove a file via the command line? Why do I have to be careful with this? 
    - $rm <file>, be careful because you can accidentally delete everything
    34. Why should I use version control? 
    - It keeps track of changes made to your code overtime, and is useful so you can revert back to specific versions
    35. How often should I commit to github?
    - commit as often as you like
    36. What is the command we would use to push our repo up to github? 
    - $ git push
    37. Walk me through Lambda's git flow. 
    - 1.  Create your own (fork) 2. Add TL 3. Clone your repo 4. Create your branch -b 'firstname-lastname' 5. Push to github. 6. Pull Req

Stretch Questions

    1. What is the difference between an inline element and a block element?
    2. What happens when an element is positioned absolutely? 
    3. How do I make an element take up only the amount of space it needs but also have the ability to give it a width? 
    4. Name 3 elements that are display block by default, 2 elements that are display inline by default and 1 element that is display inline-block by default
    5. In your own words, explain the box model. What is the fix for the box model? 

